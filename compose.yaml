services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: civiclens
      POSTGRES_USER: civiclens
      POSTGRES_PASSWORD: civiclens
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U civiclens -d civiclens"]
      interval: 3s
      timeout: 3s
      retries: 10
    volumes:
      - civiclens-postgres:/var/lib/postgresql/data

volumes:
  civiclens-postgres:

